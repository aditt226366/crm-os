import { ProductsPage } from "@/components/site/ProductsPage";

export const metadata = {
  title: "Products · WhatsApp-OS",
  description: "Products offered by WhatsApp-OS — inbox, AI agent, campaigns, ads, and more."
};

export default function Page() {
  return <ProductsPage />;
}
