import { AboutPage } from "@/components/site/AboutPage";

export const metadata = {
  title: "About · WhatsApp-OS",
  description: "About WhatsApp-OS — the multi-tenant WhatsApp AI CRM."
};

export default function Page() {
  return <AboutPage />;
}
