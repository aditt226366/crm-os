# LiveKit Agents worker (replaces the old Pipecat media service). Pinned to the
# 1.6.x line the agent.py was written+verified against — if you bump this,
# re-check the AgentSession / session.say / sarvam.* / anthropic.LLM signatures
# (see agent.py header). The four livekit packages must stay version-aligned.
livekit-agents~=1.6.6
livekit-plugins-sarvam~=1.6.6
livekit-plugins-anthropic~=1.6.6
livekit-plugins-silero~=1.6.6
httpx>=0.27
python-dotenv>=1.0
